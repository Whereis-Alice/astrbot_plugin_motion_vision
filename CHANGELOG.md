# 更新日志

## 0.5.1

- 修复 OneBot CQ 引用卡片中 `reply` 与 `json` 分段状态丢失的问题；现在编码过的卡片标题、
  描述和 B 站短链会正确作为引用内容关联到同一条视频。
- B 站字幕接口的临时失败增加 30 秒负缓存，并保留一次 429 退避重试，减少重复请求和触发限流的机会。
- 修复临时对象 id 复用可能导致消息段被跳过的问题，引用回查和结构化卡片遍历改为按当前递归路径检测循环。
- ffmpeg 超时时主动终止子进程，并在多窗口抽帧中保留已经成功取得的画面，避免后台残留进程和整段结果丢失。
- 同名但地址不同的视频不再被错误去重；混合发送直接视频和引用视频时仍会回查引用内容。
- 插件卸载时不再无限等待失联的 B 站下载线程；取消中的下载也会进入尾任务保护。
- 转写服务商、模型和密钥变化会让对应缓存自动失效；手动指定动图帧数为 1 时不再被自动下限覆盖。
- 只读取 B 站字幕而不下载视频时，不再额外注入“找不到视频文件”的误导性提示。
- AstrBot 内置 STT 与自定义转写接口共用串行闸门，并受单视频处理时间限制；转写失败会明确告知模型。
- 补充未被 Core 还原成消息组件的 OneBot 原始 file/video/CQ 段识别，纯附件消息也能进入处理流程。

## 0.5.0

- **融合 B 站视频入口**：识别 BV 号、av 号、完整链接、短链和 QQ/OneBot 引用卡片，
  下载后直接进入 Motion Vision 的统一抽帧、音轨和回看流程。
- **字幕成为画面的补充证据**：优先读取 B 站官方/AI 字幕，保留时间点并限制长度；
  视频下载失败时仍尽量把标题、简介和字幕交给模型，不让一次画面失败变成整条消息失效。
- 新增 `read_bilibili_caption` 工具，模型可以按需读取指定链接或分 P 的字幕；请求经过串行闸门、
  有界缓存和 429 退避，避免重复访问 B 站接口。
- 引用卡片解析只提取必要字段，并对短链跳转、字幕 CDN 和外部资料做域名/大小限制。
- OneBot 只给出引用 ID 时，最多进行一次有界 `get_msg` 回查；补充视频分区、发布时间、分 P、
  画面尺寸和公开数据等可验证元信息。
- 新增可选「B 站链接与字幕」配置分组；不引入扫码登录、第二套视频模型或复杂 WebUI，保持主插件配置精简。
- B 站视频下载使用 `yt-dlp`，画面仍由当前 AstrBot 模型直接接收抽出的图片。
- 修复 yt-dlp 在线程中超时后仍可能和下一次下载并行的问题；现在会等待尾部任务真正结束。
- B 站实现按解析、网络客户端和下载三个职责拆分；保留 `motion_vision.bilibili` 作为稳定门面，
  后续维护不再依赖一个难以测试的超大模块。

## 0.4.0

- **模型可以回看了**：每个动图/视频都会拿到一个短编号并留在对话历史里，之后模型可以
  主动把画面调回来重看 —— 还能指定「多给几帧」或「只看第 30 到 45 秒」。
  以前画面用完即焚，下一轮再追问「他左手拿的是什么」，模型手里已经没有画面了。
  可在「回看」分组里关闭。
- **支持只看视频的某一段**：采样窗口现在接受时间区间，帧数预算按这一段的长度重算，
  10 秒的片段不会再拿到整片的稀疏帧。
- **找不到 ffmpeg 时给出可操作的引导**：启动日志和 `/motionvision status` 会直接列出三种装法
  （装 `imageio-ffmpeg` pip 库最省事），不再只说一句「未找到」。
- `/motionvision clear` 会一并清空媒体档案；`status` 增加回看状态与已登记条数。
- 时长文案统一走一个函数，顺手修掉了 59.7 秒被写成「1 分 60 秒」的问题。
- 加了 GitHub Actions：每次 push 在 Python 3.11 / 3.12 上跑 ruff 与全部单测。

## 0.3.0

- **动图取帧改成密度驱动**：不再是每档一个固定帧数，而是和视频一样按时长算 ——
  节省约每 1.2 秒 1 帧（3~10 帧）、均衡约每 0.6 秒 1 帧（6~20 帧）、精细约每 0.35 秒 1 帧（10~30 帧）。
  一两秒的表情包帧数和 0.2.0 完全一致，十几秒以上的长动图不再只看到 6 帧。
- **动图和视频的采样参数彻底分开**，互不影响；"手动指定帧数"拆成"动图固定帧数"和
  "视频固定帧数"两项，旧配置里的单一值会自动被两边继承。
- **大文件按比例减帧**：超过 5 MB 减到 3/4、超过 10 MB 减到一半（原来固定只减 1~2 帧，
  在高帧数预算下几乎等于没减）。
- **修复**：帧延时写成 0 或缺失的动图，此前所有帧的时间戳都是 0.0s、时长识别为未知；
  现在这类延时统一按 100ms 计，时间轴恢复正常。
- 动图现在会先估算总时长再决定帧数，无需二次解码。
- `/motionvision status` 分别显示动图和视频的采样密度、帧数范围与覆盖值。

## 0.2.0

这一版重做了「一个视频该抽多少帧」的算法。旧逻辑只分「短视频 / 长视频」两档固定帧数，
结果 1 分钟的短片和 2 小时的电影会拿到完全一样的帧数，而后者平均每几分钟才有一帧，
却依然被叫做「精细」。

- **帧数改成按采样密度计算**：每个档位定的是「大约每隔几秒要有一帧」，再夹进上下限。
  10 秒的短片和 10 分钟的长片不会再拿到同样的帧数。
- **精细档明显变密**：1 分钟视频从 20 帧（每 3 秒 1 帧）提升到 40 帧（每 1.5 秒 1 帧）。
- **单轮上限放宽**：最多送出的画面 32 → 48 张，总体积 15 → 20 MB。
- **超长视频优先铺开覆盖**：超过 15 分钟的片子把每个窗口的帧数从 3 减到 2，
  用同样的总帧数换更多的采样位置。
- **修复时长探测失败时的退化行为**：以前会让 ffmpeg 连续输出原生帧，抽出来的几张图
  几乎一模一样；现在退回每 2 秒 1 帧的固定间隔。
- **多个媒体共享预算时改为份额均分**：以前先到先得，第一个视频可能吃光整轮额度，
  后面的视频一帧都拿不到。
- **超预算时改为沿时间轴均匀抽稀**，不再截断片尾 —— 以前模型只能看到视频的开头。
- **把实际采样间隔如实告诉模型**：注入的说明里会写明「平均每 X 秒 1 帧」，
  间隔过大时额外提醒「两帧之间发生的事情没有被记录，不要凭空补全」。

## 0.1.0

首个版本。

- 支持 GIF、动态 WebP、APNG 的抽帧解析。
- 支持视频抽帧，涵盖直接发送、群文件、引用消息三种来源。
- 长视频改用多窗口取样，覆盖全片的同时保留动作连续性。
- 可选提取视频音轨，支持原样附带给模型或先转写成文字。
- 三档细节预设（节省 / 均衡 / 精细），一个选项决定帧数与画质。
- 抽帧结果带缓存，重复提问同一个媒体不会重复解码。
- 单轮张数与总体积双重预算保护，超限时优先保住第一个媒体。
- 解析失败会告知模型原因，避免凭空编造画面内容。
- `/motionvision status` 查看依赖与运行状态，`/motionvision clear` 清理缓存。
